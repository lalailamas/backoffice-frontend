// import RepositionTable from '@/app/replacements/repositionTable/page'

async function header ({ restockData, stores }) {
  return (
    <div className='overflow-x-auto p-10'>
      {/* <RepositionTable data={restockData} stores={stores} /> */}
    </div>
  )
}
export default header
